import styled from "styled-components";
const Header = () => {
  return (
    <header>
      <Title>React Range Picker</Title>
    </header>
  );
};

const Title = styled.h1`
  font-size: 2em;
  font-weight: 100;
`;
export default Header;
